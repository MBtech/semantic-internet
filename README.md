# Introduction

This repository contains the code and demos for semantic internet work

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## How to run
```bash
python -m semantic_resolver.resolver
```

## Directory structure
`semantic_resolver`: contains the code for the semantic resolver including data models and query models